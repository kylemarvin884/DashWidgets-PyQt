# 2026-05-24

## 组件系统重做
- 重做 DesktopWidgetWindow.paintEvent：从浅色毛玻璃 `QColor(243,243,243,200)` 改为深色玻璃 `QColor(12,12,18,35)`，与内置组件的白色文字匹配
- 使用 GlassCard 相同的渲染风格：多层阴影叠加、顶部折射高光、半透明边框、底部暗边
- 移除 resizeEvent 中的 QRegion mask（因为有了 shadow_margin）
- 新增 9 个内置组件，使注册表覆盖全部 17 个 AVAILABLE_WIDGETS：
  - stopwatch_widget, timer_widget, pomodoro_widget, note_widget
  - exchange_widget, rss_widget, automation_widget
  - image_widget, document_viewer_widget
- 重做 registry.py：清理冗余注释，使用循环注册所有内置组件
- 更新 builtin/__init__.py：导出所有 17 个组件
- 修复关键 bug：QPointF 在 PySide6 中属于 QtCore 而非 QtGui，导致 _register_builtins 整体 import 失败、所有 17 个组件均未注册、桌面只显示组件名称 QLabel

## 组件系统全面重做（Win11 风格）
- DesktopWidgetWindow.paintEvent：从液态玻璃改为 Windows 11 实色卡片（暗色 #2c2c2c / 亮色 #f3f3f3），轻阴影+细边框
- 所有 17 个内置组件：移除白色硬编码文字，改用 `Win11Style.widget_colors()` 主题感知颜色
- Win11Style 新增 `widget_colors()` 方法：提供 title/text/text_secondary/text_dim/accent/bg_input/border_input/separator/track
- 组件用 QLabel + QSS 替代大部分 paintEvent 手绘，按钮用 QPushButton
- 保留 paintEvent 的组件：RingIndicator(CPU/MEM)、SpeedIndicator(网络)、TimerRing(番茄钟)、CoverArt、WeatherIcon、ShortcutIcon

## UI Bug 修复批量
- 移除 WidgetSettingsDialog 中 `setTheme(Theme.LIGHT)` 和硬编码 `background-color: #f5f5f5`，避免强制覆盖应用主题
- 之前已修复：组件外框透明边框、网络监控 UP/DN 错位、QFontMetrics 缺失 import、右键菜单透明边框、置底消失(HWND_BOTTOM→NOTOPMOST)、移除拖动功能/手势光标、秒表按钮去 emoji、设置不生效(key mismatch)

## 笔记组件右键菜单修复
- 禁用 QTextEdit 默认右键菜单（深色风格与 Win11 主题不搭），设置 `NoContextMenu`
- 补充 `Qt` 导入，清理未使用的 `QLabel` import

## 桌面窗口按钮点击问题全局修复
- 根因：DesktopWidgetWindow.mousePressEvent 在所有左键点击时启动拖拽，吞掉子组件（按钮、输入框等）的点击事件
- 新增 _is_interactive_widget() 检测可交互控件，点击时 return 不启动拖拽
- mouseMoveEvent 动态切换光标 — 交互控件 ArrowCursor / 其他 OpenHandCursor

## 音乐播放器组件完整修复（三轮迭代）
- 图标：FIF.PREV/NEXT 不存在 → SKIP_BACK/SKIP_FORWARD fallback 链
- 新增 media_control_service.py：纯 Win32 API 媒体控制（无额外依赖）
- **最终修复**：重写 _try_audio_sessions() COM 枚举逻辑，正确获取音频会话信息
  - 测试验证成功检测到 Spotify 正在播放的歌曲 "提瓦特民谣"
- MusicWidget 关键修复（多轮迭代）：
  - 播放图标：新增 _manual_play_state 手动状态锁，点击时立即切换图标不被轮询覆盖
  - 歌曲信息：_update_ui 放宽条件 — 只要 title 有值就显示
  - 去掉进度条、上一曲/下一曲按钮，精简为单一播放/暂停按钮
  - 切歌图标改为 PAGE_LEFT / PAGE_RIGHT 翻页风格
  - 封面图 CoverArtWidget → 替换为 MusicIcon(36px, 显示♫符号)，删除 QPixmap 依赖
  - 修复 QRectF 导入问题（PySide6 中在 QtCore 非 QtGui）
## 快捷方式小组件全面重写
- 新增 AppScanner：单例模式，扫描 Program Files/AppData/开始菜单，找到 534 个 exe/lnk
- extract_icon_from_exe：SHGetFileInfoW API 提取真实应用图标，_hicon_to_pixmap 用 GetIconInfo+GetBitmapBits 转换
  - 注意：64位系统上 HBITMAP 需用 c_void_p 类型避免溢出；paintEngine().getDC() 在 PySide6 不可用
- AddAppDialog：搜索式添加应用对话框（QListWidget + 搜索过滤）
- ShortcutItem：右键菜单支持移除；显示真实图标或首字母回退
- ShortcutWidget：右键菜单（添加应用/刷新列表）；配置持久化 settings.shortcuts
- shortcut_widget.py 修复 QRectF 导入（QtCore）

## 天气组件接入真实数据
- WeatherWidget 原来只显示硬编码 "晴 22℃"，未连接 WeatherService
- 新增 `_fetch_weather()` 方法：调用 `WeatherService.get_weather()` 获取 Open-Meteo 真实天气
- 添加 `_ICON_MAP` 映射：服务返回的 SUNNY/CLOUD/RAIN → 图标组件的 sunny/cloudy/rainy
- 启动时立即拉取，QTimer 每 30 分钟自动刷新

## 音乐组件实时刷新优化
- 刷新间隔：2.5s → 1s，提升响应速度
- 播放/暂停图标逻辑重写：
  - 点击后立即切换图标（视觉反馈）
  - 新增 3秒自动复位计时器 `_manual_timer`，之后恢复跟随实际媒体状态
  - 解决之前手动状态永久锁死、不再更新的问题
- _update_ui 状态判断优化：有歌曲显示▶(暂停态)、无歌曲显示▶(播放态)+ "未在播放"

## 之前的工作
- 重置所有配置文件到初始状态
- 修复 plugin_view.py 中 _load_recommendations 不存在的 bug（改为 refresh()）
- 修复 main.py memoryview decode lint 错误（bytes() 包装）
