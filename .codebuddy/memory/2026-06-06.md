# 2026-06-06

## 网络监控组件小数显示修复
- 问题：`SpeedIndicator` 中数字字体 17px 太大，在小部件宽度内右侧被裁剪，导致 "1.5 MB/s" 的小数部分不可见
- 修复：`network_monitor_widget.py`
  - 速度数字字体：17 → 14
  - 文字起始偏移 `tx`：36 → 30（给右侧更多空间）
  - 优化 `num_width` 用 `QFontMetrics` 提前计算

## 主页视图实时刷新重做
- `WidgetModel` 改为继承 `QObject`，新增 `widgets_changed = Signal()`，在 `activate_widget`/`deactivate_widget` 时发射
- `HomeView` 完全重写：
  - 刷新间隔：30s → **1s**（真正的实时刷新）
  - 连接 `WidgetModel.widgets_changed` 信号：组件启用/停用时**立即**刷新
  - **使用排行**作为主区域：显示前 10 名，含名次(🥇🥈🥉彩色)、图标、名称、"启用中"标签、热度进度条、时长、次数
  - **统计卡片**：`_StatCard` 支持 `set_value()` 动态更新，实时显示已激活数/可用数/总时长
  - **已启用组件**：`_ActiveChip` 带绿色状态指示灯，头部显示"共 N 个"计数
  - 主题切换时全体重建

## 系统监控组件 UI 重做
- 从双圆环布局 → **三行进度条**：CPU / 内存 / 磁盘
- 每行：标签 | ████░░ 进度条 | 百分比 | 副文本（频率/用量）
- 新增磁盘使用率监控（之前没有）
- 进度条渐变色：CPU=蓝、MEM=橙、DISK=绿
- 圆角进度条 + 线性渐变，紧凑清晰，不再拥挤

## 番茄钟组件全面完善
- `pomodoro_widget.py` 完全重写（从 159 行 → ~300 行）：
  - **重置按钮** (SYNC)：重置当前阶段计时，不切换相位
  - **进度环配色**：专注=红色(#ff6b6b)，休息=绿色(#4ecb71)
  - **系统提示音**：`MessageBeep(MB_ICONASTERISK)` 阶段结束时响铃
  - **今日统计**：显示 🍅 N 个 + 专注时长，跨天自动清零
  - **统计持久化**：`config/pomodoro.json`
  - **自定义时长**：`PomodoroSettingsDialog` 设置专注/短休/长休(1-120min)
  - **时长设置按钮**："⚙ 时长设置"，弹出主题适配对话框

## 计时器组件自定义时间功能
- `timer_widget.py` 新增预设按钮 [1分] [3分] [5分] [10分] [15分] [自定义]
- `CustomTimerDialog`：时分秒 SpinBox，主题适配（`Win11Style.c()` + `qconfig.themeChanged`）
- 显示格式升级：超过 60 分钟显示 `H:MM:SS`

## 网络组件小数显示修复
- `SpeedIndicator` 速度字体 17→14，tx 偏移 36→30，解决小数部分被裁剪



