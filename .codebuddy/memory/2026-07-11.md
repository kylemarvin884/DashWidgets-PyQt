# 2026-07-11

## UI 完全重做为 Claude 设计风格
依据 `DESIGN-claude.md` 文档全面重构 UI 设计系统：

### Win11Style 重写（核心样式系统）
- 浅色模式：奶油画布 `#faf9f5` + 珊瑚红 `#cc785c` + 暖墨黑 `#141413`
- 深色模式：深海军蓝 `#181715` + 奶油白 `#faf9f5`
- 新增 `FONT_SERIF`（Georgia 衬线）和 `FONT_SANS`（Segoe UI 无衬线）
- `label_title()` 改用衬线字体 weight 400 + 负字距
- `widget_colors()` 更新所有颜色为 Claude 色板

### 桌面小组件窗口
- `paintEvent`：从暗灰玻璃 → 奶油色卡片（`card_bg` #efe9de）
- 边框改为 `hairline` 细线色 `#e6dfd8`
- `_setup_window` 背景色从硬编码 → `Win11Style.c()['card_bg']`

### 全局 QSS（main.py + window.py 双层覆盖）
- 强制浅色主题 `Theme.LIGHT`（奶油画布是品牌核心）
- **window.py 新增 `_apply_claude_style()`**：直接覆盖 qfluentwidgets 内部样式
  - 导航面板：奶油底色 `#f5f0e8` + 右边线 + 圆角导航项
  - 卡片：`CardWidget`/`SimpleCardWidget`/`SettingCard` → 12px 圆角 + 奶油色底
  - 按钮：`PrimaryPushButton`/`PushButton` → 珊瑚红 8px 圆角
  - 输入框：`LineEdit`/`ComboBox` → 白底 + 细线边框 + 聚焦珊瑚红
  - 菜单：`QMenu`/`RoundMenu` → 白底 + 8px 圆角
  - 标签：`TitleLabel` → 衬线字体
  - 滚动条/复选框/选项卡/工具提示全部 Claude 风格
- **禁用 Mica 云母效果**（需要不透明奶油画布）
- **水印颜色**改为 Claude muted `rgba(108,106,100,180)`

### 主页视图
- 标题：28px 700 → 衬线 36px 400 负字距
- 统计卡片数值：24px 700 → 衬线 28px 400
- "使用排行"/"已启用组件" 标题改衬线 18-22px
- 绿色标记 `#4ecb71` → Claude success `#5db872`
